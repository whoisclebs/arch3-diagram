# Your first diagram

The editor is meant to play a role similar to Swagger Editor: author the source,
validate it quickly, and learn the notation through immediate feedback.

1. run `npm install`
2. run `npm run dev:editor`
3. open the `commerce.arch3.json` example
4. choose the layer you want to inspect
5. preview the current rendering output while refining the notation
